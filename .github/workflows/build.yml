name: 打包 KDENGINE（AndroLua+ Beta 壳）

on:
  push:
    paths: 'main.lua'        # 只有 main.lua 变化时才运行
  workflow_dispatch:         # 也支持手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1️⃣ 检出代码
        uses: actions/checkout@v4

      - name: 2️⃣ 设置 JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      # ---------- 安装 apktool（带备用下载）----------
      - name: 3️⃣ 安装 apktool
        run: |
          echo "📥 下载 apktool 脚本..."
          wget -q --show-progress https://raw.githubusercontent.com/iBotPeaches/Apktool/master/scripts/linux/apktool || \
          curl -L -o apktool https://raw.githubusercontent.com/iBotPeaches/Apktool/master/scripts/linux/apktool
          chmod +x apktool
          sudo mv apktool /usr/local/bin/

          echo "📦 下载 apktool.jar..."
          wget -q --show-progress https://bitbucket.org/iBotPeaches/apktool/downloads/apktool_2.9.3.jar || \
          curl -L -o apktool_2.9.3.jar https://bitbucket.org/iBotPeaches/apktool/downloads/apktool_2.9.3.jar
          sudo mv apktool_2.9.3.jar /usr/local/bin/apktool.jar

          echo "✅ 验证 apktool 版本："
          apktool --version || echo "⚠️  apktool 启动失败，请检查网络"

      # ---------- 下载官方 AndroLua+ Beta APK 壳 ----------
      - name: 4️⃣ 下载 APK 壳
        run: |
          curl -L -o template.apk \
            https://github.com/mason-lai/AndroLua-plus/releases/download/v7.0-beta5/AndroLua-plus-v7.0-beta5.apk

      # ---------- 解包 APK ----------
      - name: 5️⃣ 解包 APK
        run: |
          mkdir unpacked
          apktool d template.apk -o unpacked -f

      # ---------- 替换成你的 main.lua ----------
      - name: 6️⃣ 替换 main.lua（你的代码）
        run: |
          cp main.lua unpacked/assets/main.lua

      # ---------- 重打包为未签名 APK ----------
      - name: 7️⃣ 重打包
        run: |
          apktool b unpacked -o unsigned.apk

      # ---------- 下载通用签名工具 ----------
      - name: 8️⃣ 下载 uber-apk-signer
        run: |
          wget -q https://github.com/patrickfav/uber-apk-signer/releases/download/v1.3.0/uber-apk-signer-1.3.0.jar
          mv uber-apk-signer-1.3.0.jar uber-apk-signer.jar

      # ---------- 签名（使用 Android 调试密钥，无需创建）----------
      - name: 9️⃣ 签名 APK
        run: |
          java -jar uber-apk-signer.jar \
            --apks unsigned.apk \
            --out KDENGINE.apk \
            --ksDebug ~/.android/debug.keystore \
            --ksPass android \
            --keyPass android \
            --ksAlias androiddebugkey

      # ---------- 上传最终 APK ----------
      - name: 🔟 上传 APK 到 Actions 产物
        uses: actions/upload-artifact@v4
        with:
          name: KDENGINE-Beta
          path: KDENGINE.apk
